#!/bin/sh

cat >&2 <<'EOF'

CV workflow reminder:
  1. Push accepted CV_website commits to GitHub.
  2. Pull the iCloud production checkout before relying on cron:

     cd "$HOME/Library/Mobile Documents/com~apple~CloudDocs/Documents/CV"
     git pull --ff-only

EOF

exit 0
